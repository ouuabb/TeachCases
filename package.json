{
  "name": "teach-cases",
  "version": "1.0.0",
  "private": true,
  "description": "教学案例 monorepo — 统一管理每个教学项目",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
